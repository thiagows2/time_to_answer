<% @questions.each do |question| %>
<div class="card">
  <h3 class="card-header"> <%= question.description %></h3>
  <div class="card-body">
    <ul>
      <% question.answers.each do |answer| %>
        <li><%= answer.description %></li>
      <% end %>
    </ul>
  </div>
</div>
<% end %>